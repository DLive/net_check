[
  	{net_check, []},
  	{kernel,
  		[
  			{logger_level, info},
  			{logger,
    			[
    				{handler, default, logger_std_h,
      					#{
      						level => info,
      						formatter => {logger_formatter,
                        		#{template => [time," ",pid," ",msg,"\n"]}}
                       	}
                    },
    				{handler, file, logger_std_h,
      					#{
      						level => info, 
      						config => #{file => "/tmp/net_check.log", max_no_bytes => 1073741824, max_no_files => 5}}},
      				{handler, error_info, logger_std_h,
      					#{
      						level => error, 
      						config => #{file => "/tmp/net_error.log", max_no_bytes => 1073741824, max_no_files => 5}}}
      			]}
      	]}
].
